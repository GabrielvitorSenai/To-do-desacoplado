package com.kanban.model;

/**
 * Enum que representa os possíveis estados de uma tarefa.
 * Os valores são os mesmos usados no frontend Angular.
 */
public enum StatusTarefa {
    PENDENTE,
    EM_EXECUCAO,
    CONCLUIDA
}
